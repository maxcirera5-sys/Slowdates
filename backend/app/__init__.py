"""Slowdates backend — an AI-first dating app with no chat.

The product is two AI engines:
  * Compatibility engine  -> decides whether two people match.
  * Date-planning engine  -> decides where and when they meet.

Everything else (models, API, geo/time helpers) exists to feed those two.
"""

__version__ = "0.1.0"
